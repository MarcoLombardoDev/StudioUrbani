# Istruzioni per Claude

## Branch

Questo repository lavora su un solo branch: **`main`**. Non creare branch
di lavoro (`claude/...` o altro) per nessuna modifica: si sviluppa,
committa e pusha sempre direttamente su `main`.
